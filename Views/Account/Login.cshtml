
@{
    ViewBag.Title = "Login";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model LibraryManagement.Models.User
<link href="~/Content/custom/Login.css" rel="stylesheet" />
<div class="container-fluid">
    <div class="row">
        <div class="col-sm-9 col-md-7 col-lg-5 mx-auto">
            <div class="card card-signin my-5">
                <div class="card-body">
                    <h5 class="card-title text-center">Login</h5>

                    @using (Html.BeginForm("Login", "Account", FormMethod.Post, new { @class = "form-signin" }))
                    {

                        @Html.AntiForgeryToken()
                        <div class="form-label-group">
                            @Html.TextBoxFor(m => m.Email, new { @class = "form-control", @placeholder = "Email Address", @id = "inputEmail" })
                            @Html.ValidationMessageFor(m => m.Email, null, new { @class = "text-danger" })
                            <label for="inputEmail">Email address</label>
                        </div>

                        <div class="form-label-group">
                            @Html.TextBoxFor(m => m.Password, new { @class = "form-control", @placeholder = "Enter Passsword", @id = "inputPassword" })
                            @Html.ValidationMessageFor(m => m.Password, null, new { @class = "text-danger" })
                            <label for="inputPassword">Password</label>
                        </div>

                        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                        <button class="btn btn-lg btn-primary btn-block text-uppercase" type="submit">Login</button>

                    }

                </div>
            </div>
        </div>
    </div>
</div>

